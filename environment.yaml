name: rele-zoo
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.8
  - pytorch>=1.8
  - tensorboardX
  - pyyaml
  - pip
  - pip:
      - pytest
      - pytest-cov
      - pytest-benchmark
      - pytest-mypy
      - flake8
      - flake8-docstrings
      - mypy
      - gym